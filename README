Dockerfile.git
execute run:
docker run -v `pwd`:/data 071f512d7ace https://github.com/Dgadavin/devopsology-base-course.git
#

docker build -t add_git_repo container -f Dockerfile.git .
sudo docker build -t ggggg - < Dockerfile.git
sudo docker run --env repo_url=https://google.com.ua/9fg8th.html --env dir_path=$PWD 301d40772637
sudo docker  run  -v `pwd`:/data --env repo_url=https://github.com/Dgadavin/devopsology-base-course.git
{1}
